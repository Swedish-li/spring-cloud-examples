# spring.cloud.bootstrap.enabled=false 在 系统属性（System properties）中禁用bootstrap.yml
spring:
  application:
    name: config-client

spring.cloud:
    config:
      uri: http://localhost:9191
      profile: dev
      label: master